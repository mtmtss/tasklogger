{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and float windows",
  "windows": ["main", "float"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
